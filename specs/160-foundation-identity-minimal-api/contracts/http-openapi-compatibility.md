# HTTP and OpenAPI Compatibility Contract

All nine migrated operations are compared with immutable FastEndpoints-before fixtures. The comparison covers route/method, binding, status, body, content type, relevant headers, cookies, redirects, challenges, endpoint name/tag/operation identity, and consumed OpenAPI schemas. Every delta must match one exact documented approval, and unused approvals fail.

# Validation quickstart: Embedded runtime profile

1. Initialize a composition from `embedded-runtime@1` using bundled catalog. Plan it and confirm 16 exact IDs, profile provenance, Runtime API and file locking, and unverified host/package state without inventory. Change same-version content and remove `WorkflowsRuntimeResumption`; invalid cases refuse.
2. Generate candidate from disposable two-environment object-map bundle. Review redacted edits, re-read selected candidate for exact IDs and compare unselected files byte-for-byte. Repeat with array source, base-disabled feature and overlay-only setting object; unsupported cases refuse without output.
3. Supply named SQLite resource, isolated writable locks directory and host-owned signing values to generic fixture. Run `Explicit_embedded_closure_uses_file_locking_without_test_provider` in `EmbeddedFixtureHostEvidenceTests`. Verify activation, suspend, bookmark resume, completion and no ASP.NET listener. Run missing-lock negative.
4. Run affected Planning/CLI/Architecture tests, `dotnet run --project tools/maps/Elsa.Maps.Generator -- check`, and solution-filter check. Inspect portable artifacts for connection/signing/lock-path values.

See [contract](contracts/embedded-profile-v1.md). This guide makes no claim of deployed candidate or verified Workbench generation.

# Research: `dotnet elsa persistence` execution model, module descriptor, and the Nuplane gap

**Spec**: [spec.md](./spec.md)

**Verified against**: `origin/main` at `43d88cef4`, 2026-09-19 (re-verified after a rebase onto this commit, 2026-09-20; R5 re-verified against `origin/main` at `e7b775832`, 2026-09-22), plus a local checkout of the `valence-works/nuplane` repository (`git describe`: `0.0.10-10-g5c0e04e`, branch `pr-67`), 10 commits ahead of the `0.0.9-preview.61` tag Elsa's `Directory.Packages.props` pins, for the R1–R3 facts below. On 2026-09-20 the Nuplane facts for R4 and the U1–U4 delivery were separately re-verified against Nuplane `main` at `e93ad89`, published as `0.0.11-preview.83`. Every Nuplane fact below carries the relevant version caveat once, here: **the R1–R3 citations are what `pr-67` did at the time it was read; the R4 and delivery citations are what `main` at `e93ad89` does today**. U1–U4 are released; the Elsa pin bump from `0.0.9-preview.61` to `0.0.11-preview.83`, tracked by [#1893](https://github.com/elsa-workflows/elsa-foundation/issues/1893), merged 2026-09-20 as [PR 1903](https://github.com/elsa-workflows/elsa-foundation/pull/1903), unblocking every dependent Elsa slice. U5 is still open (valence-works/nuplane#77) and not required. On 2026-09-22 the R5 facts below were verified against Nuplane `main` at `e49642e`, published as `0.0.11-preview.84` and pinned in Elsa by [PR 1932](https://github.com/elsa-workflows/elsa-foundation/pull/1932): **every R5 citation is that commit**, read from the same read-only checkout.

---

## How this was verified

Every Elsa-side citation below was re-opened in this worktree at the commit above and the line still matches. Every Nuplane citation was read from the Nuplane checkout, read-only, and is cited as plain text (`nuplane: <path>:<line>`), never as a relative markdown link, because that repository is not part of this tree and a link would 404. Two of those citations are worth stating precisely because they matter later in this document: the lock-file gap lives at `nuplane: src/Nuplane/Reconciliation/LockFileCoordinator.cs:23` (the check is `if (_options.Mode == LockFileMode.Generate)`), and the hardcoded host-provided allowlist is `IsSharedHostContractPackage` at `nuplane: src/Nuplane/Reconciliation/PackageDependencyGraphResolver.cs:514-533`, called from `IsHostProvidedDependency` at `:498`.

---

## Inventory of existing building blocks

[`tools/ef/module-migrate.sh`](../../tools/ef/module-migrate.sh) already does most of the mechanical work this issue asks for, for the 13 module contexts the tooling catalog enumerates — 12 with four provider-derived contexts each, plus Secrets with its single MySQL context, i.e. the 49 `ModuleDesignTimeFactory<TContext>` lines in `ModuleDesignTimeFactories.cs`:

- `script` and `script-check` produce and verify the exact `<Module>/<Provider>.sql` idempotent layout the issue proposes ([`tools/ef/module-migrate.sh:116-130`](../../tools/ef/module-migrate.sh)), one file per module context, each recording into its own `__EFMigrationsHistory_*` table.
- SQLite is already refused for scripting, with the same reasoning this spec keeps: `SqliteHistoryRepository.GetEndIfScript` throws `NotSupportedException` ([`tools/ef/module-migrate.sh:51-58`](../../tools/ef/module-migrate.sh)).
- `apply` and `validate` take the connection string as **positional argument 3** ([`tools/ef/module-migrate.sh:46`](../../tools/ef/module-migrate.sh), the `apply|validate)` case), which is why the issue's "credentials never in process arguments" acceptance line does not describe the tooling as it exists today — see *Corrections to the issue text* in [spec.md](./spec.md).
- Scripting always passes `--idempotent` ([`tools/ef/module-migrate.sh:120`](../../tools/ef/module-migrate.sh)).
- [`tools/ef/README.md:41-102`](../../tools/ef/README.md) documents the layout, the `script`/`script-check` contract, and the SQLite refusal in prose (`:69-74`); `:76-77` records that Secrets ships only a MySQL context in this shared catalog, because Secrets keeps a separate historical migration chain under `tools/ef/dual-migrate.sh`. (`tools/ef/README.md` is unmoved by the recent changes on `main` described below.)

**Why it cannot serve package consumers.** The script drives `dotnet ef`, which needs a design-time startup project and source projects to build:

- Its startup project is [`tools/ef/Elsa.EntityFrameworkCore.Tooling`](../../tools/ef/Elsa.EntityFrameworkCore.Tooling/Elsa.EntityFrameworkCore.Tooling.csproj), a compile-time catalog: [`ModuleDesignTimeFactories.cs`](../../tools/ef/Elsa.EntityFrameworkCore.Tooling/ModuleDesignTimeFactories.cs) holds 49 `ModuleDesignTimeFactory<TContext>` lines, one per provider-derived context, and its `Program.cs` reflects over them to answer `list`. All four `Elsa3Import*` factory lines are already among them ([`:9-12`](../../tools/ef/Elsa.EntityFrameworkCore.Tooling/ModuleDesignTimeFactories.cs)).
- Each context's project is located by `find "${module_roots[@]}" -name "$assembly.csproj"` ([`tools/ef/module-migrate.sh:96`](../../tools/ef/module-migrate.sh)), where `module_roots` is `src` plus `extensions` when the latter exists ([`tools/ef/module-migrate.sh:34-35`](../../tools/ef/module-migrate.sh)). This was a `src`-only `find` as recently as this spec's first draft; [PR #1867](https://github.com/elsa-workflows/elsa-foundation/pull/1867) widened it before this unit's design work concluded, so the `Elsa3Import*` contexts (under `extensions/Elsa3/src/...`) resolve a project today. This closes what an earlier draft of this document called an open defect; it is no longer part of this work's scope (see *Corrections to the issue text* in [spec.md](./spec.md)).
- An Elsa 4 host built from packages has neither a source tree nor `dotnet-ef` available to it: it has compiled module assemblies loaded by Nuplane, and nothing else. There is no `dotnet elsa` tool, and no supported way for such a host to script SQL for the modules it actually has installed. This is independent of the `find` fix above: even with every context's project resolvable, `dotnet ef` still needs the design-time startup project and source projects a packaged host does not have.

## Four places, one declaration

Adding a module, or checking that one module's identity is consistent, means touching (or auditing) four independent places today, each of which encodes the same handful of facts about that module on its own:

| # | Place | What it declares | Example |
|---|---|---|---|
| 1 | `<Module>EfModule.cs` | `HistoryModuleName` (frozen) and table/column name constants | [`RuntimeEfModule.cs:11`](../../src/Elsa/Workflows/Runtime/Persistence/EntityFrameworkCore/RuntimeEfModule.cs) |
| 2 | `<Module>EntityFrameworkCoreRegistration.cs` | a private `static readonly EfModuleBinding` built by hand, naming the owner, history table, and migrations assembly | [`SecretsEntityFrameworkCoreRegistration.cs:15`](../../src/Elsa/Secrets/Persistence/EntityFrameworkCore/DependencyInjection/SecretsEntityFrameworkCoreRegistration.cs) |
| 3 | `tools/ef/Elsa.EntityFrameworkCore.Tooling/ModuleDesignTimeFactories.cs` | one `ModuleDesignTimeFactory<TContext>` line per provider-derived context, needed for migration generation | [`ModuleDesignTimeFactories.cs:37-40`](../../tools/ef/Elsa.EntityFrameworkCore.Tooling/ModuleDesignTimeFactories.cs) (the four `Runtime*DbContextFactory` lines) |
| 4 | `tests/.../Migrations/Tests/ModuleContextCatalog.cs` | the history table name, **re-derived from the context class name** rather than read from the module | [`ModuleContextCatalog.cs:91-92`](../../tests/Elsa/Persistence/EntityFrameworkCore/Migrations/Tests/ModuleContextCatalog.cs) |

Place 4 does not read place 1's constant; it computes `EfMigrationsHistory.TableName(context.Name[..^("SqlServerDbContext".Length)])`, which for Runtime yields `__EFMigrationsHistory_Runtime`, while the host actually uses `RuntimeEfModule.HistoryTableName` = `__EFMigrationsHistory_ElsaRuntime` ([`RuntimeEfModule.cs:19`](../../src/Elsa/Workflows/Runtime/Persistence/EntityFrameworkCore/RuntimeEfModule.cs)). Nothing currently notices the mismatch, because place 4's `HistoryTable` helper is used only to build placeholder options for model-building, not to assert equality against place 1. The `[EfModule]` descriptor this spec proposes is read by all four consumers, which is what actually closes the gap rather than just moving it.

## Shell features that register EF module migrations

Built by grepping `AddEfModuleMigrations<` across `src/` and `extensions/` (excluding `README.md`/`EXTENSION_POINTS.md` prose mentions and the `EfModuleMigrator.cs`/`EfProviderBindingValidator.cs` definitions themselves), plus a check for any feature that depends on a module context without registering migrations. This is the complete input to `[UsesEfModule]` coverage (spec.md FR-064, which states `[UsesEfModule]` is `AllowMultiple` and cross-project; FR-066, which requires a guard test to fail when a feature takes a module dependency with no matching `[UsesEfModule]`): every row is a feature the activation guard must recognize.

| Feature class | Project | Context(s) | Module(s) | Own `Provider` setting (file:line) |
|---|---|---|---|---|
| `RuntimeEntityFrameworkCoreFeature` | `Elsa.Workflows.Runtime.Persistence.EntityFrameworkCore` | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeEntityFrameworkCoreFeature.cs:30` (`= "Sqlite"`) |
| `RuntimeBookmarksEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeBookmarksEntityFrameworkCoreFeature.cs:24` |
| `RuntimeActivityExecutionEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeActivityExecutionEntityFrameworkCoreFeature.cs:23` |
| `RuntimeOperationalStateEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeOperationalStateEntityFrameworkCoreFeature.cs:21` |
| `RuntimeWorkflowAlterationEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeWorkflowAlterationEntityFrameworkCoreFeature.cs:17` |
| `RuntimeWorkflowExecutionEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeWorkflowExecutionEntityFrameworkCoreFeature.cs:21` |
| `RuntimeWorkflowTestScopeEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeWorkflowTestScopeEntityFrameworkCoreFeature.cs:17` |
| `RuntimeArtifactsEntityFrameworkCoreFeature` | same | `RuntimeDbContext` | `Workflows.Runtime` | `RuntimeArtifactsEntityFrameworkCoreFeature.cs:25` (`string? Provider`, no declared default; effective default is `Provider ?? "Sqlite"` at `:69` and `:73`) |
| `DistributedRuntimeExecutionPlacementEntityFrameworkCoreFeature` | `Elsa.Workflows.Runtime.Distributed.Persistence.EntityFrameworkCore` | `ExecutionPlacementDbContext` | `Workflows.Runtime.Distributed.Placement` | `DistributedRuntimeExecutionPlacementEntityFrameworkCoreFeature.cs:21` |
| `DistributedRuntimeExecutionCommandTransportEntityFrameworkCoreFeature` | same | `ExecutionCommandTransportDbContext` | `Workflows.Runtime.Distributed.CommandTransport` | `DistributedRuntimeExecutionCommandTransportEntityFrameworkCoreFeature.cs:21` |
| `IdentityIamEntityFrameworkCoreFeature` | `Elsa.Foundation.Identity.Persistence.EntityFrameworkCore` | `IdentityIamDbContext` | `Identity.Iam` | `IdentityIamEntityFrameworkCoreFeature.cs:19` |
| `AspNetCoreIdentityEntityFrameworkCoreFeature` | `Elsa.Foundation.Identity.AspNetCoreIdentity.EntityFrameworkCore` (a **different project** from the row above) | `IdentityIamDbContext` (via the fully-qualified `Elsa.Foundation.Identity.Persistence.EntityFrameworkCore.IdentityIamDbContext`) | `Identity.Iam` | `AspNetCoreIdentityEntityFrameworkCoreFeature.cs:21` |
| `IdentityProviderConfigurationEntityFrameworkCoreFeature` | `Elsa.Foundation.Identity.Persistence.EntityFrameworkCore` | `IdentityProviderConfigurationDbContext` | `Identity.ProviderConfiguration` | `IdentityProviderConfigurationEntityFrameworkCoreFeature.cs:19` |
| `StudioPreferencesEntityFrameworkCoreFeature` | `Elsa.Studio.Preferences.Persistence.EntityFrameworkCore` | `StudioPreferencesDbContext` | `Studio.Preferences` | `StudioPreferencesEntityFrameworkCoreFeature.cs:23` |
| `PublishingEntityFrameworkCoreFeature` | `Elsa.Workflows.Publishing.Persistence.EntityFrameworkCore` | `PublishingSnapshotReviewDbContext` | `Workflows.Publishing` | `PublishingEntityFrameworkCoreFeature.cs:21` |
| `WorkflowsDesignEntityFrameworkCoreFeature` | `Elsa.Workflows.Design.Persistence.EntityFrameworkCore` | `WorkflowsDesignDbContext` | `Workflows.Design` | `WorkflowsDesignEntityFrameworkCoreFeature.cs:17` |
| `EfOpenTelemetryFeature` | `Elsa.Diagnostics.OpenTelemetry.Persistence.EntityFrameworkCore` | `OpenTelemetryDbContext` | `Diagnostics.OpenTelemetry` | `EfOpenTelemetryFeature.cs:20` |
| `StructuredLogsEntityFrameworkCoreFeature` | `Elsa.Diagnostics.StructuredLogs.Persistence.EntityFrameworkCore` | `StructuredLogsDbContext` | `Diagnostics.StructuredLogs` | `StructuredLogsEntityFrameworkCoreFeature.cs:23` |
| `ActivitiesDesignEntityFrameworkCoreFeature` | `Elsa.Activities.Design.Persistence.EntityFrameworkCore` | `ActivitiesDesignDbContext` | `Activities.Design` | `ActivitiesDesignEntityFrameworkCoreFeature.cs:13` |
| `Elsa3ImportActivitiesEntityFrameworkCoreFeature` | `Elsa3.Activities.Design.Import.Persistence.EntityFrameworkCore` (`extensions/Elsa3/`) | `Elsa3ImportDbContext` | `Elsa3.Activities.Design.Import` | `Elsa3ImportActivitiesEntityFrameworkCoreFeature.cs:22` |
| `SecretsEntityFrameworkCoreFeature` | `Elsa.Secrets.Persistence.EntityFrameworkCore` | `SecretsDbContext` | `Secrets` | `SecretsEntityFrameworkCoreFeature.cs:24` (`= "Sqlite"`); `ConfigureServices` calls `AddSecretsEntityFrameworkCore`, whose `SecretsEntityFrameworkCoreRegistration` calls `AddEfModuleMigrations<SecretsDbContext>` (`SecretsEntityFrameworkCoreRegistration.cs:38`) rather than the feature class calling it inline, the way every row above does |
| `WorkflowsDashboardEntityFrameworkCoreFeature` | `Elsa.Workflows.Dashboard.Persistence.EntityFrameworkCore` | `RuntimeDbContext` and `WorkflowsDesignDbContext` (read-only; no context or migrations of its own) | `Workflows.Runtime` **and** `Workflows.Design` (two modules, one feature) | **no `Provider` setting; skipped by the agreement comparison** — it calls neither `AddEfModuleMigrations` (confirmed: no match in its project); `ConfigureServices` ([`WorkflowsDashboardEntityFrameworkCoreFeature.cs:24-30`](../../src/Elsa/Workflows/Dashboard/Persistence/EntityFrameworkCore/WorkflowsDashboardEntityFrameworkCoreFeature.cs)) instead throws `InvalidOperationException` from `AddWorkflowRunHealthEntityFrameworkCore` ([`WorkflowRunHealthEntityFrameworkCoreRegistration.cs:20`](../../src/Elsa/Workflows/Dashboard/Persistence/EntityFrameworkCore/DependencyInjection/WorkflowRunHealthEntityFrameworkCoreRegistration.cs)) or `AddWorkflowPortfolioEntityFrameworkCore` ([`WorkflowPortfolioEntityFrameworkCoreRegistration.cs:21,27,32,38`](../../src/Elsa/Workflows/Dashboard/Persistence/EntityFrameworkCore/DependencyInjection/WorkflowPortfolioEntityFrameworkCoreRegistration.cs)) if the Runtime and Workflows Design EF backends are not already registered. This is the only feature in the table with no `Provider` setting; every other row above declares one (`RuntimeArtifactsEntityFrameworkCoreFeature`'s is nullable with no inline default, but it is still a declared setting, defaulted to `Sqlite` at its two call sites). |

Twenty-one features call `AddEfModuleMigrations<T>`, covering all 13 modules; twenty of them, each in its own file, call it directly, and `SecretsEntityFrameworkCoreFeature` reaches it one hop away, through `SecretsEntityFrameworkCoreRegistration.AddSecretsEntityFrameworkCore`. `WorkflowsDashboardEntityFrameworkCoreFeature` is the one feature that depends on module contexts (two of them) without registering migrations of its own, and is the motivating case for `[UsesEfModule]` being `AllowMultiple`. **Secrets is no longer outside this mechanism** (#1877): `SecretsEntityFrameworkCoreFeature` is annotated `[UsesEfModule("Secrets")]` and its `ConfigureServices` reaches `AddEfModuleMigrations<SecretsDbContext>` as above; `SecretsEfMigrationHostedService`, which used to drive Secrets' migrations directly, was retired by #1877's post-migration slice in favor of the same mechanism every row above already uses.

## R1 — RESOLVED: the pinned install layout is `store-state.json`, not the lock file

- Install layout: `nuplane: src/Nuplane/Feeds/PackageInstallStore.cs:36-40` (`GetInstallDirectory`) — `<installRoot>/<feedName>/<packageId>/<version>/`, a raw extracted nupkg, casing preserved by `SanitizePathSegment`; completion marker `.nuplane-ready` (`:20`, `IsInstalled` at `:48`); default root `AppContext.BaseDirectory/.nuplane/packages` when `PackageInstallRoot` is unset (`:26-29`).
- The active set persists in the state file, not the lock file: `nuplane: src/Nuplane/Store/State/StoreStateRecord.cs:16-23` carries `ActivePackageDescriptorsById`, each entry a public `nuplane: src/Nuplane.Abstractions/ActivePackageDescriptor.cs:19-32` record carrying `InstallPath`. `StoreStateSerializer` (public, camelCase JSON) and `StoreRegistry`'s public constructor `(IStoreStateSerializer, string?)` (`nuplane: src/Nuplane/Store/State/StoreRegistry.cs:28`) need no DI container to read it back. `EffectiveStorePersistenceSettings.Resolve(StoreRegistryOptions)` (`nuplane: src/Nuplane/Store/State/EffectiveStorePersistenceSettings.cs:70`) resolves the effective state-file path the same way the host does.
- The lock file is not that set: `LockFileMode.Generate` never writes it (`nuplane: src/Nuplane/Reconciliation/LockFileCoordinator.cs:23`), and nothing under `src/` calls `LockFileStore.WriteAsync` — every call site found is a test.
- Only `nuplane: src/Nuplane/Operational/ActivePackageCatalogMapper.cs:7` (`internal static class ActivePackageCatalogMapper`) is internal among these types; everything else U1 needs is already public.

**Conclusion**: an offline reader of `store-state.json` (U1) can be built entirely from types already public in the `Nuplane` package, without DI and without touching the lock file. That reader was delivered as `NuplaneStore` (U1; `nuplane: src/Nuplane/NuplaneStore.cs:14` at `e93ad89`).

## R2 — RESOLVED: `HostIntegrated` loads into a private, non-collectible ALC, not the default context

- `PackageLoadMode` has exactly two values, `Collectible` and `HostIntegrated` (`nuplane: src/Nuplane.Loading.Abstractions/PackageLoadMode.cs:6-16`); "dependency closure" is a load-planning scope, not a third mode.
- `HostIntegrated` packages load into `HostIntegratedPackageGraphLoadContext`, a `sealed internal` non-collectible subclass of `PackageGraphLoadContext` (`nuplane: src/Nuplane.Loading/HostIntegratedPackageGraphLoadContext.cs:6-11`), constructed in `PackageLoader.EnsureGraphLoaded` when `graphLoadMode == PackageLoadMode.HostIntegrated` (`nuplane: src/Nuplane.Loading/PackageLoader.cs:265-268`).
- Assemblies in that context become visible to ordinary by-name resolution (`AppDomain.GetAssemblies()`, `Assembly.Load`, `Type.GetType`) through a `Default.Resolving` hook: `nuplane: src/Nuplane.Loading/HostIntegratedAssemblyResolver.cs:26` (`AssemblyLoadContext.Default.Resolving += ResolveFromHostIntegratedPackages;`). That is exactly the resolution path [`EfRelationalProviderBinding`](../../src/Elsa/Persistence/EntityFramework/EfRelationalProviderBinding.cs) uses (`Type.GetType` at `:178`, `AppDomain.CurrentDomain.GetAssemblies()` at `:240`, `Assembly.Load(engine.PackageId)` at `:250`), so a worker that boots the real Nuplane loader binds providers exactly as the host does; a hand-rolled resolver would not go through this hook and could disagree.
- Corrected 2026-09-20 (read at `e93ad89`): native-library probing and RID-graph expansion live in `PackageGraphLoadContext`, not in `PackageLoader` (`nuplane: src/Nuplane.Loading/PackageGraphLoadContext.cs:86-98`, `LoadUnmanagedDll`/`ResolveNativeLibraryPath`, `:128` `ExpandRuntimeIdentifiers`, `:152` the `runtimes/<rid>/native` probe). `PackageLoader` keeps TFM selection and main-assembly choice as private statics (`ResolveAssemblySelection`/`ResolveMainAssemblyPath`, `nuplane: src/Nuplane.Loading/PackageLoader.cs:944-963`; `ResolveHostFramework` `:1193`) and selects RID-specific *managed* `runtimes/<rid>/lib` assets by calling `PackageGraphLoadContext.ExpandRuntimeIdentifiers` (`:998`). Also now stale: the earlier claim here that "every public call site passes `null`" for the TFM override — `hostTargetFrameworkOverride` is a `PackageLoader` constructor parameter (`:50`) reachable publicly through `HostIntegratedLoadOptions.TargetFrameworkOverride`.
- Added 2026-09-20 (read at `e93ad89`): load mode is decided per graph, not per package: one `HostIntegrated` member promotes the whole graph (`nuplane: src/Nuplane.Loading/PackageLoader.cs:186`). Visibility to by-name resolution needs the resolution catalog's `PublishGraph` as well as the hook (`PackageLoader.cs:398` `ValidateCanPublishGraph`, `:416` `PublishGraph`); the hook alone resolves nothing for a graph that was never published. This is one more reason Elsa must call the delivered entry point instead of assembling loader pieces itself.

**Conclusion**: the worker cannot reimplement this loader cheaply or safely; it must reuse Nuplane's own composition (U2) to get provider binding that matches the real host. This is now met by the delivered `NuplaneHostIntegratedLoader` (U2).

## R3 — RESOLVED: the package-manifest generator does not touch the tool project

`.github/workflows/packages.yml:99` builds its pack set with `find src extensions -name '*.csproj'` (excluding `src/Apps/*` and `*/tests/*`), so a project physically under `src/` is packed. The open question was whether the separate manifest-generation step (`Elsa.Platform.PackageManifest.Generator`, referenced by the `[ManifestFeatureCategory]` attributes seen on `SecretsEntityFrameworkCoreFeature`) has its own project-shape assumptions that would exclude a CLI/tool project such as `src/Elsa/Cli/Elsa.Cli.csproj` (a `PackAsTool`, no `[ShellFeature]`) from the manifest it emits.

Now settled by observation: `src/Elsa/Cli/Elsa.Cli.csproj` (delivered in #1874) carries no `PackageReference` and no `ProjectReference` to `Elsa.Platform.PackageManifest.Generator` — its only references are `System.CommandLine` and its own `Worker\Elsa.Cli.Worker.csproj` — and it packs as a clean `PackAsTool`/`ToolCommandName=dotnet-elsa` tool with `PackageId=dotnet-elsa`, no `[ShellFeature]` and no manifest artifacts. The generator never runs against this project, so slice 4 needed no manifest-generator change; slice 11 (`elsa-package.json` / `extensions.efModules`, #1881) depended on a separate upstream generator change of its own, not on anything this project does. That change (elsa-workflows/elsa-specifications#1, PR 2, published as `Elsa.Specifications.PackageManifest.Generator` `0.0.1-preview.8`) landed and Elsa's `Directory.Packages.props` already pins it, so slice 11 is no longer gated on an upstream change — it delivered 2026-09-22.

## R4 — RESOLVED: Nuplane's offline mode does not load from state; `NuplaneHostIntegratedLoader.LoadFromStateAsync` does

The question was whether composing Nuplane with an existing `OfflineMode`-shaped option, without registering the hosted reconciliation service, already lets the worker load the active package set "from state, offline, without reconciling" for `--packages` and any non-Nuplane-managed root, or whether U2 needed a distinct new entry point.

- Offline mode only shapes feed candidate selection. `FeedResolutionOptions.OfflineMode` and `RemoteFallbackMode` (`nuplane: src/Nuplane/Feeds/Configuration/FeedResolutionOptions.cs:80,85`) are read in exactly two places under `src/`: candidate ordering, local feeds first (`nuplane: src/Nuplane/Feeds/Policy/FeedResolutionPolicy.cs:49-50`), and disabling remote feeds (`nuplane: src/Nuplane/Feeds/MultiFeedPackageResolver.cs:421-438`, `IsRemoteFeedDisabled`). Neither gates reconciliation.
- One reconcile pass always writes. `CleanupMiddleware` calls `storeRegistry.PersistActiveVersionsAsync` unconditionally (`nuplane: src/Nuplane/Reconciliation/Middleware/CleanupMiddleware.cs:36`), rewriting `store-state.json`; the pass runs package resolution against the configured feeds (`nuplane: src/Nuplane/Reconciliation/Middleware/PackageResolutionMiddleware.cs:11`); and `PackageInstallStore.InstallAsync` extracts into, moves into, and can delete under the install root (`nuplane: src/Nuplane/Feeds/PackageInstallStore.cs:79-142`; `ZipFile.ExtractToDirectory` `:90`, `Directory.Move` `:125`, `Directory.Delete` `:107,142`). Consequence: an out-of-process tool sharing the host's directories must never reconcile.
- `LoadFromStateAsync` reads the state file strictly read-only (it is the same read as `NuplaneStore.ReadStateAsync`, `NuplaneHostIntegratedLoader.cs:158,180`; the class doc states "The entry point never writes", `:35`) and is the only variant with guaranteed grouping parity with the host, because only the state file carries the graph activation records (`PackageGraphGrouping.ForActiveState`). `LoadActivePackagesAsync` groups by graph generation identity only (documented at `NuplaneHostIntegratedLoader.cs:57-72`), which can differ from the host when the store holds several active graph records that share packages.

**Conclusion**: no — Nuplane's existing offline mode does not do what U2 needed, and composing the reconciler "offline" is unsafe for a tool. The delivered `NuplaneHostIntegratedLoader.LoadFromStateAsync` is the answer: a distinct public entry point that reads the state file read-only and never reconciles. Elsa's worker must call `LoadFromStateAsync`, not `LoadActivePackagesAsync`, whenever a state file exists, and must never register or invoke reconciliation itself.

**Residual risks**

- The load is irreversible for the process lifetime (`NuplaneHostIntegratedLoader.cs:29-33`): host-integrated graphs load into non-collectible contexts (`nuplane: src/Nuplane.Loading/HostIntegratedPackageGraphLoadContext.cs:12`, `isCollectible: false`) and the `AssemblyLoadContext.Default.Resolving` hook (`nuplane: src/Nuplane.Loading/HostIntegratedAssemblyResolver.cs:26`) is never removed by this entry point. Acceptable for a short-lived worker that exits; it rules out a long-running tool process that reloads package sets. The load is also not re-entrant (a process-wide lock; a nested call from a gate throws `InvalidOperationException`, `NuplaneHostIntegratedLoader.cs:37-41`). Elsa's worker must do exactly one load per process and exit afterward.
- There is a TFM override (`HostIntegratedLoadOptions.TargetFrameworkOverride`, `nuplane: src/Nuplane.Loading/HostIntegratedLoadOptions.cs:32`) but deliberately no RID override (`:27-29`): native libraries and `runtimes/<rid>/lib` managed assets follow the worker process's own `RuntimeInformation.RuntimeIdentifier`. The default host TFM (`PackageLoader.ResolveHostFramework`, `nuplane: src/Nuplane.Loading/PackageLoader.cs:1193`, derived from the running process when no override is given) is already correct for a worker launched on the host's own runtimeconfig (spec FR-002). Elsa's worker must pass no `TargetFrameworkOverride`.
- A state-file read racing a host write can throw `IOException` or `JsonException`: the host writes the state file in place with `File.Create` (`nuplane: src/Nuplane/Store/State/StoreStateSerializer.cs:51`), not write-to-temp-and-move, so a reader can see a locked, empty or torn file. `NuplaneStore` documents both exceptions as transient (`nuplane: src/Nuplane/NuplaneStore.cs:54-55,68-69`). Elsa's worker must retry a bounded number of times, then fail with exit 3.

## R5 — RESOLVED: yes, once, only when the operator asks — an opt-in `--restore`

A host whose install root has never been populated (a fresh remote-feed host that has not yet run its first reconcile) has an empty package directory, so the worker would find nothing to script against. It was open whether an opt-in `--restore` flag should run a Nuplane reconcile (network access, no loading) before reading the state, or whether the tool's "never downloads by default" rule (D10) should extend to "never downloads, period."

**Settled** by the owner on 2026-09-21 ([#1927](https://github.com/elsa-workflows/elsa-foundation/issues/1927)): the tool may touch the network **when, and only when, the operator asks it to**. D10's default is unchanged — no command downloads anything, and no flag implies `--restore`. Four points were settled with it, and are what the implementation has to be:

1. **Pinned only.** `--restore` refuses, before any network call, when any desired package is not a single-point version pin. A remote feed already needs literal ids with a range to resolve anything, so this costs little. The consequence is the reason for the rule: a restored artifact is byte-identical to a started host's for the same pins, so `migration-plan.json` needs no "restored" field, `script-check` needs no special case, and the frozen `EfToolingHost` contract is untouched.
2. **Never-started hosts only, under an upstream store lock.** `--restore` acts only when no active package set is recorded; on a populated host it does nothing and says so. Every reconcile now holds a store lock, so a tool write cannot tear a host's state file — and a restore that cannot take that lock does nothing rather than waiting or writing.
3. **Feeds with credentials are refused by name**, with the reason. Credential resolution is its own upstream follow-up.
4. **Paths.** The worker reads the host's `appsettings.json` and its environment overlay for the `Nuplane` section, and passes absolute paths derived from `--host`, so nothing lands in the tool's own directory.

**What the upstream API gives us** (Nuplane `0.0.11-preview.84`, `main` at `e49642e`, pinned in `Directory.Packages.props` by [#1932](https://github.com/elsa-workflows/elsa-foundation/pull/1932)). No host-free "reconcile, do not load" entry point existed at `0.0.11-preview.83`, and composing one in Elsa would have meant encoding Nuplane internals here — loading left unregistered, the queue-and-wait ingress never completing without a started host, and three path defaults resolving against the *calling* process, so an unconfigured host would have been restored into the tool's own directory. It was therefore filed and delivered upstream (valence-works/nuplane#82), matching D12's standing rule, and Elsa uses it rather than composing anything:

- `NuplaneRestore.RestoreAsync(IConfiguration, NuplaneRestoreOptions?, ct)` (`nuplane: src/Nuplane/NuplaneRestore.cs`) accepts the host's configuration root *or* its `Nuplane` section, runs exactly one reconciliation cycle through `IReconciliationService` with the loading module's auto-loading observer never registered, and returns `NuplaneRestoreResult` — the paths actually used, the active set read back, `IsDegraded`, `FailedPackages`, `CredentialRefusedFeeds`, and `Skipped`/`SkipReason`.
- `NuplaneRestore.DescribeDesiredAsync` is a write-nothing, no-remote-contact pre-flight listing every request with `IsPinned`, plus `CredentialRefusedFeeds` and `SourceErrors`. Elsa runs it first, which is what lets the credential refusal happen **before** anything is downloaded rather than after.
- `NuplaneRestoreOptions.BasePath` replaces every path default that would otherwise resolve against the restoring process; a path nothing pins is an `InvalidOperationException` rather than a silent write into the tool's directory. Elsa sets `BasePath` to the `--host` directory and pins `StateFilePath` to `NuplaneInstallRoot.DefaultStateFile(hostDirectory)`, which is the one state file the tool reads back. It sets **no `InstallRoot` override**: with `BasePath` given, Nuplane's own default is `BasePath/.nuplane/packages`, which is exactly where a started host — resolving against its own `AppContext.BaseDirectory` — puts it, and nothing in `NuplaneInstallRoot` expects a different layout (it derives install paths from the state file's own records, not from a root of its own).
- `RequirePinnedVersions` makes point 1 a property of the call rather than of Elsa's own parsing: the include-pattern parser and the version classifier that decide pinned-ness are internal to Nuplane (`nuplane: src/Nuplane/Versioning/NuGetVersionRequestClassifier.cs`), and a `Skipped` result with `UnpinnedRequests` is returned before anything is resolved, downloaded, installed or written.
- `ConfigureBuilder: Action<NuplaneBuilder, IConfiguration>` is how directory feeds are added, because core `AddNuplane` skips every feed declaring a directory path. Its **second** argument is the already-resolved `Nuplane` section; a captured configuration root would make `AddDirectoryFeedsFromConfiguration` find no keys at all.
- The store lock (`nuplane: src/Nuplane/Store/State/StoreLock.cs`) is an exclusive `FileShare.None` handle on `<state file>.lock`. Acquisition never waits: a cycle that cannot take it returns `Skipped` with `StoreLockUnavailable` and does nothing. That is the whole of point 2's running-host defense, and Elsa attempts no other liveness detection.

**What one pass actually writes**, re-read at `e49642e` and narrower than ADR 0076's original prose: the resolved state file, the store lock file beside it, and each acquired package extracted under the install root plus a `.tmp` staging directory. Cleanup during a cycle produces decisions and **deletes nothing** (`nuplane: src/Nuplane/Reconciliation/Middleware/CleanupMiddleware.cs`); the only deletes anywhere on the path are the pass's own staging directory and an extraction that never completed — a directory carrying no `.nuplane-ready` marker, which is by definition not an installed package (`nuplane: src/Nuplane/Feeds/PackageInstallStore.cs`, `TryPublish`). The earlier "can extract into or delete under the install root" was therefore stricter than this version's behavior, and both `NuplaneLoader`'s remark and ADR 0076 D10 now say the narrower thing.

**One trap worth recording.** A directory feed's configured path is resolved with `Path.GetFullPath` at registration time (`nuplane: src/Nuplane.Sources.Directory/Registration/DirectorySourceRegistrationServices.cs`), i.e. against the *restoring process's* current directory, not against `BasePath`. `Elsa.Foundation.Host`'s own `appsettings.json` configures `"DirectoryPath": "packages"`, so a tool run from anywhere else would resolve it against the operator's shell. The worker therefore sets its current directory to the `--host` directory for the duration of the restore and restores it afterwards; every other path the run uses is already absolute.

---

## U1–U5 evidence table

| # | Change | Evidence it is needed | Delivered (2026-09-20, Nuplane `e93ad89`, `0.0.11-preview.83`) |
|---|---|---|---|
| U1 | Named offline reader for the installed set | Nothing today reads `store-state.json` without pulling in reconciliation plumbing; a hand implementation would mean parsing JSON Nuplane already owns the schema for, or referencing all of `Nuplane` plus its `NuGet.Protocol` dependency just to read a state file. | valence-works/nuplane#73, delivered through PR 79 (`7fe4e10`): `NuplaneStore` (public static class, `nuplane: src/Nuplane/NuplaneStore.cs:14`) — `ReadActivePackagesAsync(stateFilePath, ct)` (`:70`), `ReadStateAsync(stateFilePath, ct)` (`:100`), `GetActivePackages(StoreStateRecord)` (`:136`), plus `StoreRegistryOptions` overloads of both reads (`:166`, `:191`). Opens the file `FileAccess.Read` with `FileShare.ReadWrite \| FileShare.Delete` (`:112-113`); a missing file is an empty result (`:115`), except the `StoreRegistryOptions` overloads throw `InvalidOperationException` when no state file path resolves (`:205`). |
| U2 | Host-free "load this active set" entry point | `EfRelationalProviderBinding` resolves types by `Type.GetType`, `AppDomain.GetAssemblies()`, and `Assembly.Load` (`:178, 240, 250`); those only see `HostIntegrated` assemblies through the `Default.Resolving` hook Nuplane installs (R2). A worker that loads assemblies any other way binds providers differently from the real host. | valence-works/nuplane#74, delivered through PR 81 (`e93ad89`): `Nuplane.Loading.NuplaneHostIntegratedLoader` (public static class, `nuplane: src/Nuplane.Loading/NuplaneHostIntegratedLoader.cs:45`) — `LoadFromStateAsync(string stateFilePath, HostIntegratedLoadOptions?, ct)` (`:151`), `LoadFromStateAsync(StoreRegistryOptions, HostIntegratedLoadOptions?, ct)` (`:175`), `LoadActivePackagesAsync(IReadOnlyList<ActivePackage>, HostIntegratedLoadOptions?, ct)` (`:116`). Result: `HostIntegratedLoadResult` (`nuplane: src/Nuplane.Loading/HostIntegratedLoadResult.cs:21`). Options `HostIntegratedLoadOptions` (`nuplane: src/Nuplane.Loading/HostIntegratedLoadOptions.cs:10`) expose `TargetFrameworkOverride` (`:32`), `ActivationGates` (`:47`), `SharedAssemblies` (`:55`), `LoggerFactory` (`:62`); grouping uses `PackageGraphGrouping.ForActiveState` (`nuplane: src/Nuplane.Loading/PackageGraphGrouping.cs:40`, internal). |
| U3 | Pre-activation gate contract | Before delivery: `INuplaneObserver` exceptions are swallowed (`nuplane: src/Nuplane/Events/ObserverEventDispatcher.cs:16-45`, every observer call wrapped in `try`/`catch` that only logs); `IPackageLoadModeAdvisor` is advice-only (`nuplane: src/Nuplane.Loading/PackageLoader.cs:37` as a constructor parameter, consulted for mode selection, never for a block/allow decision); `PackageTransactionRequest.StageExecutor` (`nuplane: src/Nuplane/Store/Transactions/PackageTransactionRequest.cs:24`) is declared but never set by any caller. Nothing in Nuplane could stop a package from loading. | valence-works/nuplane#75, delivered through PR 80 (`45360ee`): `IPackageActivationGate` (`nuplane: src/Nuplane.Loading.Abstractions/IPackageActivationGate.cs:40`), `EvaluateAsync(PackageActivationContext, ct)` returning `PackageActivationGateResult` (`:51`); `PackageActivationGateResult.Allow` / `.Block(reason)` (`nuplane: src/Nuplane.Loading.Abstractions/PackageActivationGateResult.cs:18,40`). Gates run inside `PackageLoader.EnsureGraphLoadedAsync` (`nuplane: src/Nuplane.Loading/PackageLoader.cs:120`), after graph load-mode selection (`:136`, `SelectGraphAsync`), at `:204-229`, before load-context construction (`:402-403`). A `Block` is reported as an ordinary load failure; gates fail closed. The host-free loader takes gates through `HostIntegratedLoadOptions.ActivationGates`. |
| U4 | Register `DesiredManifestPackageSource` | `nuplane: src/Nuplane/Sources/DesiredManifestPackageSource.cs:12` is implemented and (per the plan) tested, but grepping Nuplane's composition (`AddNuplane` and friends) found no registration of it as an `IDesiredPackageSource`; setting `Convergence:Manifest:Enabled` did nothing. | valence-works/nuplane#76, delivered through PR 78 (`0232eac`): `DesiredManifestPackageSource` is now registered as an `IDesiredPackageSource` (`nuplane: src/Nuplane/Registration/NuplaneDesiredStatePlanningRegistrationServices.cs:16-20`). It is always registered; `ReconciliationService` skips it while its `IsEnabled` is false (`nuplane: src/Nuplane/Reconciliation/ReconciliationService.cs:75-79`), so `Convergence:Manifest:Enabled` now has an effect. |
| U5 | Schema-v2 `nuplane.json` capability declaration | [`docs/foundation-host-feeds.md:151`](../../docs/foundation-host-feeds.md) documents today's workaround in prose: the provider engine "must be named by hand" when generating a host's package closure, because no package declares "I need one of these engines, chosen by configuration." | Still open (valence-works/nuplane#77). Not required for #1861; recorded as a follow-up (N5) because nothing currently in Nuplane's manifest schema expresses a capability choice — this is new design, not a composition change, which is why it is sized L and deferred. |

## Two adjacent Nuplane defects (out of scope for #1861)

Found while reading the areas above; neither blocks this issue, and neither is proposed as Nuplane work here — they are recorded so the issue comment can mention them without conflating them with U1–U5:

- **The lock file is dead code for writing.** `LockFileMode.Generate` never writes (`nuplane: src/Nuplane/Reconciliation/LockFileCoordinator.cs:23`), and only test code calls `LockFileStore.WriteAsync`. Nuplane's own roadmap already has a line item close to this: `nuplane: specs/028-operational-stability-roadmap/roadmap.md:366`, "Track 7 - Lock File as Offline Execution Contract".
- **A hardcoded, Elsa-specific host-provided allowlist.** `PackageDependencyGraphResolver.IsSharedHostContractPackage` (`nuplane: src/Nuplane/Reconciliation/PackageDependencyGraphResolver.cs:514-533`) lists thirteen `Elsa.*` package ids, three `CShells.*.Abstractions` ids, two `Nuplane.*.Abstractions` ids, and an `Microsoft.Extensions.` prefix, by name, in a general-purpose package manager. This is adjacent to, but distinct from, U4/U5: it is a dependency-resolution concern, not a load-mode or capability concern.

Nuplane's own spec for host-integrated loading already names migrations assemblies as the motivating case for that mode: `nuplane: specs/018-host-integrated-loading/spec.md:147` ("SC-003: A package containing database migrations can be loaded in host-integrated mode and its migrations assembly can be resolved by framework code by name"), which is independent confirmation that this design direction (reuse Nuplane's loader rather than re-implement it) is the one Nuplane's own authors had in mind for exactly this scenario.

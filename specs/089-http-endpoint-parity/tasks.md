# Tasks: HTTP Endpoint Full Parity — Sub-unit C (parsing, auth, faults, limits)

**Input**: Design documents from `/specs/089-http-endpoint-parity/` (plan.md §C milestone row, research.md D6, contracts/internal-seams.md §C, spec.md User Story 3 / FR-011..FR-014)

**Status note**: Sub-unit A merged (PR #578); sub-unit B implemented on `089b-routing-upgrade` (PR #585, checks green, awaiting merge — this branch is STACKED on it and rebases onto main when #585 lands). B delivered (template, method) identity, binding metadata (`Elsa.Http.Core.HttpEndpointRouting` vocabulary), the per-shell route table + `IWorkflowTriggerIndexObserver`, middleware template resolution + 409 ambiguity guard, and RouteData. This pass covers **sub-unit C only**, branch `089c-parsing-auth-faults`.

**Tests**: Included (QA gate; spec US3 defines acceptance).

**Descoped (spec Assumptions)**: multipart/form-data file-upload validation; quiescence gate; correlation selectors.

## Phase 1: Setup

- [X] T001 Confirm clean baseline on `089c-parsing-auth-faults` (stacked on 089b): `dotnet build Elsa.Server.slnx` (SDK 10 at /usr/local/share/dotnet) — baseline build green (0 warnings, 0 errors)

## Phase 2: Foundational (contracts + identity metadata other tasks build on)

- [X] T002 Move the endpoint policy CONTRACTS to `Elsa.Http.Core` (pre-release move, impls stay put): `IHttpEndpointAuthorizationHandler` + `AuthorizeHttpEndpointContext`, `IHttpEndpointFaultHandler` + `HttpEndpointFaultContext`, and `HttpBadRequestException` migrate from `src/Elsa/Workflows/Runtime/Http/{Contracts,Models,Exceptions}` to `src/Elsa/Http/Core/` (namespaces follow); `Elsa.Workflows.Runtime.Http` keeps the implementations (`AuthenticationBasedHttpEndpointAuthorizationHandler`, `AllowAnonymousHttpEndpointAuthorizationHandler`, `HttpEndpointFaultHandler`) and its reflective type-name feature settings. Rationale: the middleware (in `Elsa.Activities.Http`) must consume the contracts without a cross-module edge — same placement logic as `HttpEndpointRouting`. Check `AuthorizeHttpEndpointContext`/`HttpEndpointFaultContext` member types for anything Runtime-specific (e.g. WorkflowState) and slim them to what the middleware can actually supply (HttpContext + policy / fault info); update both EXTENSION_POINTS.md files
      — Note: moved contracts to `Elsa.Http.Core` ({Contracts,Models,Exceptions}); slimmed `AuthorizeHttpEndpointContext` to `(HttpContext, Policy)` (dropped Runtime `Workflow` resource; handler now authorizes user against policy alone, documented in XML remarks); `HttpEndpointFaultContext` unchanged (stale WorkflowState remark removed); no csproj change (HttpContext resolves transitively via Routing.Abstractions); both EXTENSION_POINTS.md updated.
- [X] T003 Extend the shared vocabulary + provider for non-identity endpoint options: add `AuthorizeMetadataKey`/`PolicyMetadataKey`/`RequestTimeoutMetadataKey`/`RequestSizeLimitMetadataKey` to `src/Elsa/Http/Core/HttpEndpointRouting.cs` (values `http:authorize` etc. per data-model.md); `HttpEndpoint` (`src/Elsa/Activities/Http/Activities/HttpEndpoint.cs`) gains literal-at-publish inputs `Authorize` (bool, default false), `Policy` (string?), `RequestTimeout` (TimeSpan?), `RequestSizeLimit` (long?); `HttpEndpointTriggerStimulusProvider` reads them (non-literal → publish fails, same rule as Path/SupportedMethods; absent → omitted from metadata) and stamps them on every descriptor's metadata — NON-IDENTITY: they must NOT enter `HttpEndpointStimulus.Hash`
      — Note: options carrier `HttpEndpointStimulusOptions` (renamed to avoid clash with existing middleware `HttpEndpointOptions`); metadata values authorize `"true"` (omit-when-false), policy raw, requestTimeout invariant TimeSpan `"c"` (`00:00:30`), requestSizeLimit invariant long; stamped on all method descriptors, absent omitted; hash untouched.
- [X] T004 [P] Provider/identity unit tests for T003 in `tests/Elsa/Activities/Http/Tests/`: options stamped on all method descriptors; hash unchanged with/without options (identity invariance pin); non-literal option throws; §E2.8 note — new inputs reconcile via the CLR source (no picker-side change)

**Checkpoint**: contracts compile from `Elsa.Http.Core`; options ride binding metadata; identity invariant pinned.

## Phase 3: User Story 3 — Parsed content, authorization, faults, limits (Priority: P3)

**Goal**: Authorize-marked endpoints 401 anonymous callers; oversized bodies 413 per-endpoint; JSON bodies surface parsed on `ParsedContent`; faults map to 408/400/500.

**Independent Test** (spec US3): authorized endpoint anonymously → 401 (no dispatch); valid JSON → ParsedContent populated; body over the per-endpoint limit → 413; faulting dispatch → mapped status.

### Implementation

- [X] T005 [US3] Request-body parse entry point in `src/Elsa/Http/` (research D6): inspect the prioritized `IHttpContentParser` set (response-message-shaped, used by SendHttpRequest) and add a request-side entry point that reuses the same selection strategy WITHOUT regressing the response path — e.g. a small `IHttpRequestBodyParser` (contract in `Elsa.Http.Core`, impl in `Elsa.Http`, registered by `HttpFeature`) that takes (contentType, body string) and returns a wire-safe `JsonElement?` (JSON → parsed element; text/* → string element; unknown → null). Catalogue in `src/Elsa/Http/EXTENSION_POINTS.md`. Parser unit tests incl. a SendHttpRequest-response-path regression pin
      — Note: added `IHttpRequestBodyParser.Parse(string? contentType, string body): JsonElement?` (`Elsa.Http.Core`) + default `HttpRequestBodyParser` (`Elsa.Http`, public, `TryAddSingleton` in `HttpFeature`); shape mismatch documented in class remarks — response set is stream/`object`-shaped so a small content-type dispatch is used instead of reusing them verbatim (json/`+json`/`text/json` → parsed, malformed → null, charset tolerated; `text/*` → string element; unknown/absent/empty → null); EXTENSION_POINTS.md replacement entry added; 15 parser tests incl. a public-`XmlHttpContentParser` response-path regression pin; Http.Tests 31/31 green, solution build 0/0.
- [ ] T006 [US3] Middleware enforcement (`src/Elsa/Activities/Http/Middleware/HttpEndpointMiddleware.cs`) — the claimant-binding lookup from the B ambiguity guard now also supplies the endpoint options (single-definition claimants; take the matched (template, method) binding's metadata): (a) **Authorize**: when `http:authorize` = true, resolve `IHttpEndpointAuthorizationHandler` (GetService — absent handler = fail closed) and evaluate with the policy; failure → 401 before body read or dispatch. (b) **RequestSizeLimit**: per-endpoint value overrides `HttpEndpointOptions.MaxRequestBodyBytes` for the bounded body read (413 unchanged). (c) **ParsedContent**: parse the body via `IHttpRequestBodyParser` (Content-Type header) into `HttpRequestModel.ParsedContent: JsonElement?` (wire-optional). (d) **RequestTimeout + fault mapping**: linked CTS (request-aborted + per-endpoint timeout) around the dispatch; wrap dispatch in try/catch → `IHttpEndpointFaultHandler` maps (OperationCanceled/Timeout → 408, `HttpBadRequestException` → 400, other → 500); success paths unchanged
- [ ] T007 [US3] Surface `ParsedContent` on the activity (`src/Elsa/Activities/Http/Activities/HttpEndpoint.cs`): `OutputArgument<object?> ParsedContent` (value = the JsonElement from the stimulus model; null-safe fallback like RouteData); wire-optional `ParsedContent` member on `HttpRequestModel`
- [X] T008 [US3] Investigate + wire per-shell authentication for the authorization handler: `AuthenticationBasedHttpEndpointAuthorizationHandler` needs `HttpContext.User`; Program.cs runs `UseAuthentication` on the ROOT pipeline while shell middleware runs in the shell branch — verify how W4's FastEndpoints per-shell security populates identity (see `tests/Elsa/Api/FastEndpoints/Tests/PerShellApiSecurityIntegrationTests.cs`) and make the handler evaluate authentication explicitly (`AuthenticateAsync` against the shell's schemes) rather than assuming middleware ordering; set an explicit `IMiddlewareShellFeature.Order` on `ActivitiesHttpFeature` only if the investigation shows shell-pipeline auth middleware exists to order against (else document why default 0 stands)
      — Note: investigation — root `UseAuthentication` (placed after `MapShells`) authenticates against shell schemes and populates `HttpContext.User` BEFORE the shell branch runs, so `User` IS normally populated; auth is NOT a shell middleware feature, so `IMiddlewareShellFeature.Order` (which only sequences shell middleware features relative to each other) buys nothing → default 0 stands, reasoning documented in `ActivitiesHttpFeature.UseMiddleware`. Handler hardened: uses the populated principal when authenticated, else calls `HttpContext.AuthenticateAsync()` (shell default scheme via RequestServices), fails closed on any auth/policy failure or throw; policy via `IAuthorizationService`. Handlers made `public` (guard-safe, no InternalsVisibleTo) for testability. Added `Microsoft.AspNetCore.Authentication.Abstractions` (2.3.11) + test `Microsoft.AspNetCore.Http`. Tests: handler suite 17/17 (7 new), Activities.Http 59/59 + IntegrationTests 8/8, arch guard 49/49, full build 0/0.
- [X] T009 [P] [US3] Middleware unit tests (`tests/Elsa/Activities/Http/Tests/HttpEndpointMiddlewareTests.cs`): authorize=true + failing handler → 401, router untouched, body unread; authorize=true + passing handler → 202; per-endpoint size limit overrides the global (both directions); ParsedContent populated for application/json + absent for unknown content type; timeout → 408 via fault handler (fake router that delays); HttpBadRequestException from dispatch → 400; unexpected dispatch exception → 500; options absent → today's behavior unchanged
      — Note: added 13 tests + `EndpointEnforcementTestDoubles.cs` (BehaviourStimulusRouter delaying/throwing — the shared RecordingStimulusRouter can't delay/throw; FakeAuthorizationHandler recording policy; FakeFaultHandler writing a sentinel). Fail-closed 401 covers BOTH no-RequestServices and failing-handler; a TrackingStream pins body-unread on the no-RequestServices 401; passing handler asserts the policy string flowed from metadata + same HttpContext. Size-limit both directions (smaller→413, larger→202). ParsedContent uses the REAL `HttpRequestBodyParser` (added `Elsa.Http` ref to the test csproj): json→object, unknown→JSON null. Faults via inline fallback (408/400/500) + one asserting a registered fault handler runs INSTEAD (teapot sentinel + exception surfaced). Options-absent regression: the pre-existing 57 tests all still pass. Activities.Http.Tests 70/70.
- [X] T010 [US3] Integration tests (`tests/Elsa/Activities/Http/IntegrationTests/`): fixture gains a test authentication scheme + the real `AuthenticationBasedHttpEndpointAuthorizationHandler` and `HttpEndpointFaultHandler` registrations; scenarios — authorized endpoint anonymous → 401 & no instance; authenticated → 202; JSON body → run's Result.ParsedContent holds the parsed structure; per-endpoint RequestSizeLimit → 413
      — Note: added 4 tests. Fixture gains `TestAuthHandler` (`Authorization: Test <name>` scheme) + `AddAuthentication/AddAuthorization` + `UseAuthentication` before the endpoint middleware (mirrors prod root-pipeline order) + REAL `AuthenticationBasedHttpEndpointAuthorizationHandler`/`HttpEndpointFaultHandler`/`HttpRequestBodyParser` registered explicitly (feature reflective loader can't resolve out-of-assembly types). `PublishHttpEndpointWorkflowAsync` overload authors Authorize/Policy/RequestSizeLimit literals (same shape as SupportedMethods) → the trigger provider stamps them on binding metadata end to end. "No instance" asserted via new `CountWorkflowExecutionsAsync` over `IWorkflowExecutionStateStore` (==0). ParsedContent read from the run's durable `Result.ParsedContent`. Added `Elsa.Http` ref to the integration csproj. Small src change: made `HttpEndpointFaultHandler` `public` (was `internal`, no InternalsVisibleTo) so the fixture can register it — same testability rationale as T008's public auth handlers; arch guard 49/49 unaffected. IntegrationTests 12/12.

## Phase 4: Polish & Cross-Cutting

- [X] T011 [P] (done: internal-seams §C as-built — contract move, parser seam, options metadata, enforcement order, auth investigation result; surface doc 401/413/408/400/500 + activity rows live, stale duplicate row removed) — Docs: contracts/internal-seams.md §C as-built (contract moves, parser seam, fail-closed auth, options metadata); contracts/http-endpoint-surface.md (401/413/408/400/500 rows live; activity surface rows for Authorize/Policy/RequestTimeout/RequestSizeLimit/ParsedContent); EXTENSION_POINTS.md updates land with T002/T005
- [X] T012 (full-solution build 0W/0E; 52/52 test projects green incl. architecture guard) — QA gate: full `dotnet build` + full `dotnet test Elsa.Server.slnx` + architecture guard green

## Dependencies

- T002 → T006/T008 (contracts location); T003 → T004, T006, T007
- T005 → T006(c) → T007; T006 → T009; T006+T008 → T010
- T011/T012 last; T004/T005 parallel after T003; T009 parallel with T010 prep

## Implementation Strategy

Foundational contracts/metadata first (T002–T004), then the parser seam (T005) and middleware enforcement (T006–T008), tests alongside, one PR through the QA gate (based on #585's branch until it merges, then rebased onto main).

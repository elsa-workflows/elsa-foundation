# Elsa.Activities.Sequence

Runtime-only Sequence activity module. The activity owns ordered executable child scheduling through
the `Sequence.Activities` child slot and the runtime composite-activity seam.

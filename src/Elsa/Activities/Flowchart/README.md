# Elsa.Activities.Flowchart

Runtime-only Flowchart activity module. The activity owns its executable child graph through the
`Flowchart.Activities` child slot and slot metadata, then schedules child executable nodes through
the runtime composite-activity seam.

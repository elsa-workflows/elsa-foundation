<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <PackageReference Include="Nuplane.Admin" />
  </ItemGroup>

  <ItemGroup>
    <!-- Minimal-API endpoint mapping (IEndpointRouteBuilder, Results, HttpContext, IFormFile, [FromServices]). -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Core\Elsa.Modularity.Core.csproj" />
    <ProjectReference Include="..\..\Git\Elsa.Git.csproj" />
    <ProjectReference Include="..\..\Foundation\Identity\Abstractions\Elsa.Foundation.Identity.Abstractions.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Documented exception to the §2.23.3 no-InternalsVisibleTo rule: the ExtensionBuilder subsystem
      is a large, host-private surface (~80 interlocking internal types) whose contracts are not meant
      for consumption outside this module. Publicizing it to satisfy tests would promote a host-private
      API into the public surface. Tracked by the InternalsVisibleTo allow-list guard test
      (ArchitectureGuardTests.InternalsVisibleTo_occurrences_are_limited_to_documented_exceptions).
    -->
    <InternalsVisibleTo Include="Elsa.Modularity.Tests" />
  </ItemGroup>

</Project>

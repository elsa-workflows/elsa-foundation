{
  "Elsa": {
    "Persistence": {
      "EntityFramework": {
        "Migrate": {
          "// Policy": "Validate refuses to start while a module context has a pending migration, so a deployment applies them before the host runs, from CI/CD: dotnet elsa persistence apply, then dotnet elsa persistence post-migrate for any module reporting a required post-migration action. Every EF module reads this one key, Secrets included. AutoMigrate hands migration back to shell activation.",
          "Policy": "Validate"
        }
      }
    }
  }
}

# Spikes

Disposable, self-contained experiments. They are **not** product code.

- Not in `Elsa.Server.slnx`.
- Ignored by the EF Core surface ratchet (`spikes/`).
- Pruned from all-project zero-EF restore.

Delete or promote a spike when it has answered its question. Do not wire a spike into Workbench.

<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- The CLR scanner reads this assembly's version as the default activity version
         (US1). Pin it to 2.1.0 and keep the informational version free of a +hash
         suffix so the resolver sees a clean SemVer. -->
    <Version>2.1.0</Version>
    <IncludeSourceRevisionInInformationalVersion>false</IncludeSourceRevisionInInformationalVersion>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\..\..\..\..\src\Elsa\Activities\Runtime\Core\Elsa.Activities.Runtime.Core.csproj" />
  </ItemGroup>

</Project>

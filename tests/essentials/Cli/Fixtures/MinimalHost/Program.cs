// A fixture host is never started. It exists to be *built*: what the tool inspects is its published output
// — a runtimeconfig, a deps file, and the module and provider-engine assemblies beside them — which is all
// a packaged host offers a tool that has no source tree.
return 0;

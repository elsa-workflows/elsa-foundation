// A fixture host is never started. It exists to be *built*: what the tool inspects is its published output
// — a runtimeconfig, a deps file, the module and provider-engine assemblies beside them, and the
// shells.json the provider-agreement check reads.
return 0;
